## MONOLOTHIC APP DEOPLOY

1. Change the image and post also the port or we can use config and secret also
2. Then hit the code like this,

- kubectl apply -f app.yaml -f config.yaml -f secret.yaml
- kubuctl apply -f .