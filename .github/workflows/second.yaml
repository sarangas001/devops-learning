name: Second CI

on:
  workflow_dispatch

jobs:
    build:
        runs-on: ubuntu-latest
    
        steps:
            - name: Say Hello
              run: echo "Hello from the terminal second workflow!"